baseURL: "https://cuisine.restanrm.fr/"
languageCode: "fr-fr"
title: "Recettes de Cuisine"
theme: "explore"

#sectionPagesMenu: "main"

outputs:
  home:
    - HTML
    - RSS
    - JSON

taxonomies:
  categories: categories
  tags: tags

menu:
  main:
    # - name: Accueil
    #   url: /
    #   weight: 1
    # - name: Search
    #   url: /search
    #   weight: 2
    # - name: Categories
    #   url: /categories
    #   weight: 3
    # - name: Tags
    #   url: /tags
    #   weight: 4
    - name: All posts
      url: /posts/
      weight: 5

params:
  author: "Adrien Raffin"
  description: "Recettes de cuisine à partager en famille"
  ShowPostTags: true
  ShowBreadCrumbs: true

  ui:
    stickyHeader: false
    showSearch: true
    searchEngine: fuse

  sidebar:
    globalPosition: "left"
    widgets:
      - recent
      - categories
      - tags
  single:
    toc:
      enable: false
